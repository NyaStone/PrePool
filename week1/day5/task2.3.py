print(list(map(lambda x: x * x, [3, 2, 6, 7, 1, 4])))
# lambda is used to make a anonymous function
# map applies a function to a list
# list to convert the map object back into a list for ease of use (maps are more efficient but less readable)

